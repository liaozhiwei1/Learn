RPC实现需要解决的问题:
    1.代理问题  代理模式    通过代理将网络链接等通用的部分影藏起来，就像使用feign时你不用关心网络请求。只需要写出服务名称，参数,地址等
    2.序列化问题  jdk序列化机制 json
    3.通信问题  使用何种网络通信方式，包括：tcp（直接通过套接字）或者http（标准协议）的选择
    4.服务实例化问题   反射，在运行时才知道操作的是什么，并且可以在运行时获取类的完整构造，并调用对应的方法。让登记的服务变成可以实际执行的对象实例。

猜测:
    feign请求过程：通过动态代理为每一个定义的接口生成对象（代理问题），对象包含服务名称，服务地址，
    然后通过注册中心拿到服务实例注册表，通过服务名称拿到对应的服务ip和端口（服务实例化问题）
     然后使用统一的序列化方法进行序列化甚至是压缩参数（序列化问题），
     最后通过网络编程（有ip和端口）调用对应的服务（通讯问题）